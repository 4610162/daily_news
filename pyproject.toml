[project]
name = "news"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "google-generativeai>=0.7.2",
    "feedparser",
    "python-telegram-bot",
    "python-dotenv",
    "PyGithub",
    "fredapi",
    "yfinance",
    "pandas",
    "toml",
    "pyyaml",
    "mkdocs-material",
]
